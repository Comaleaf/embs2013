#include "vlab.hch"
#include "fsl.hch"
#include "main.hch"

void main(void) {
	unsigned 32 datin;
	
	initialise();
	
	par {
		uart_driver();
		fsl_driver();
		while (1) {
			fsl_read ? datin;
			dac_write_to_output(0xF, datin <- 12);
		}
	}
}
