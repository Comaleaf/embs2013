#include "vlab.hch"
#include "fsl.hch"
#include "dac.hch"
#include "main.hch"

ram <unsigned 12> buffer;

void main(void) {
	unsigned 32 datin;
	
	initialise();
	
	par {
		uart_driver();
		fsl_driver();
		while (1) {
			fsl_read ? datin;
			dac_write ! (datin <- 12);
		}
	}
}